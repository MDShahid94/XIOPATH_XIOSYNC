# CHECKPOINT — live micro-state (crash-recovery cursor)

> Unlike HANDOFF-LOG.md (append-only ledger), this file is **overwritten
> freely** and describes only the *current instant*. It exists so that an
> agent killed mid-task loses at most ONE step of work. Rules in
> SESSION-PROTOCOL.md §5. Keep it under ~25 lines — it is a cursor, not a log.

- **Status:** IN-PROGRESS
- **Session:** 018 (fresh duplicated workspace; §7 drill run: sentinel set
  10.0 → continue, symlinks + import OK, local Postgres 16.12 reinstalled
  at localhost:5432, `origin` is a v0 bundle — push via PAT inline)
- **Task:** Phase 1 first slice — five identity tables (STATE.md next action
  item 2): ORM models in `persistence/models/`, immutable `organization_id`
  (C2), RLS (doc 05 §3.2), Alembic revision 0002, `target_metadata` wired
  (INV-TEST-SCHEMA-2 drift half).
- **Branch:** `phase1/identity-tables` off `main` @ `56f94da` (canonical
  `main` is protected — PR when green). Workspace note: `origin` and
  `shahid` here both point at canonical `MDShahid94/XIOSYNC_V0`; the old
  mirror repo is not configured in this workspace.
- **Step plan:**
  1. ✅ Branch created; checkpoint committed.
  2. ✅ ORM models (5 tables, naming convention, same-org composite FKs,
     org-leading indexes) + `env.py` `target_metadata` wired; proof
     (re-run green in Session 017 recovery): ruff + format + mypy --strict
     (42 files) + lint-imports (4 kept) + `pytest tests/tools tests/unit`
     (67 passed) @ commits `2af19b7`+`cd07d64`.
  3. ✅ Revision 0002 (tables + RLS policies + immutability triggers, DDL
     autogenerated from metadata; harness head-check made dynamic); proof:
     ruff + format + mypy --strict (26 files) + lint-imports (4 kept) +
     `pytest tests -q` (69 passed, incl. 0001..0002 roundtrip on local
     Postgres 16.12) @ commit `05b44c0`.
  4. ✅ Drift test (autogenerate diff empty) + RLS/immutability integration
     tests (plain-role fixtures in conftest); proof: ruff + format + mypy
     --strict (27 files) + lint-imports (4 kept) + `pytest tests -q`
     (74 passed) @ commit `5bccd28`.
  5. Push branch to canonical, open PR, verify GitHub CI green (D-017),
     merge into protected `main`, record run ID.
  6. Handoff per SESSION-PROTOCOL §3 (log, STATE, CHECKPOINT → IDLE).
- **Cursor:** step 5 (next agent starts here).
- **Local DB note:** in-sandbox Postgres 16.12 (UTF8) at
  `localhost:5432`, data dir `/tmp/pgdata` (reinstalled this session via
  `dnf install postgresql16-server`; `initdb -U postgres --auth=trust`,
  `pg_ctl -D /tmp/pgdata start`; `DATABASE_URL` appended to
  `.env.development.local`). No Neon integration in this workspace;
  GitHub CI remains the authoritative postgres:17 proof (D-016).
